server:
  port: 9092

spring:
  datasource:
    url: jdbc:postgresql://localhost:5435/otllo
    username: admin
    password: password
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
